<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.globalgates.mapper.NewsLikeMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into tbl_news_like (member_id, news_id)
        values (#{memberId}, #{newsId})
    </insert>

    <delete id="deleteByMemberIdAndNewsId">
        delete
        from tbl_news_like
        where member_id = #{memberId}
          and news_id = #{newsId}
    </delete>

    <select id="selectByMemberIdAndNewsId">
        select id, member_id, news_id, created_datetime
        from tbl_news_like
        where member_id = #{memberId}
          and news_id = #{newsId}
    </select>

    <select id="selectCountByNewsId" resultType="int">
        select count(*)
        from tbl_news_like
        where news_id = #{newsId}
    </select>

</mapper>
